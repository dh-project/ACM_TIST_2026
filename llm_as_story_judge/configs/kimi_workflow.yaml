workflow:
  exp_name: baseline
  logical_model_key: "kimi"

  dataset_path: "judge_data/dataset60.json"
  schemas_path: "baseline_assets/schemas/"
  prompts_path: "baseline_assets/prompts/"

  run_name: null
  batch_size: 30
  max_retries: 5

  reasoning_enabled: true   
  verbose_llm: true

  base_output_dir: "experiments"
